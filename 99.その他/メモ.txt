1.Eclipseのワークスペースビルドやサーバー起動で、
なんかよく行けなかったら、 プロジェクト全体のSVNロック解除して再実行

2.MyBatisのSQLでパラメータを持って来られなくて、
Could not set parameters for mappingか、
SQLException: Invalid column type: 1111が出ていたら、
インスタンスの渡しが出来ていないことだから、コントローラーやサービスの方をチェックしろ

3.Mattermostのコードブロック機能
グレイヴ・アクセント 3個 (` ` `)
[内容]
グレイヴ・アクセント 3個 (` ` `)

4.画面でボタンを押せない、なんか反応がない、JSPやJS側の問題っぽい
⇒ブラウザの開発者ツール(F12)でコンソールを確認しろ！

5.sudo (SuperUser DO, Linuxコマンド)
管理者権限でアカウント制限を無視し、別にアカウントを設定して強制実行する時に使われる。 
本来のアカウントに入って実行する時は必要ない。

※　「一般User・権限なし」でログインした場合のバッチ実行
権限ないので、sudoを使ってbatch26(SuperUser)として実行。
sudo -u batch26 ./ADM_USR_B_62011A.sh

※　「SuperUser・root・権限あり」でログインした場合のバッチ実行
権限あるので、なんにも無しで。
./ADM_USR_B_62011A.sh